<!doctype html>
<html>
<head>
    <title>PERSONAL LIBRARY</title>
    <link rel="stylesheet" href="//netdna.bootstrapcdn.com/bootstrap/3.0.2/css/bootstrap.min.css">

    <script src="https://kit.fontawesome.com/b1767921c9.js" crossorigin="anonymous"></script>

    <link rel="stylesheet" href="profilestyle.css">
</head>
<body>
<div class="container">

    <div class="page-header text-center header">
        <h1><span class="fa solid fa-arrow-down"></span> SEARCH UP A BOOK BY AN ISBN NUMBER AND RATE IT!</h1>
        <a href="/logout" class="btn btn-default btn-sm">Logout</a>
    </div>

    <section class ="adder">
    <h1 class ="link1"><a href ="https://isbnsearch.org/">USE THIS LINK TO GRAB ISBN NUMBERS YOU NEED!</a></h1>
    <% if (user.local.email) { %>
        <input type="text" placeholder="ISBN NUMBER HERE!" name="BOOK" id="lookUp" class="lookUp">
        <button type="submit" id ="submit">Submit</button>
      
       <div id ="displayHere"></div>
    </div>
    <% } %>
    
<!--CREATE ANOTHER CRUD METHOD THAT WOULD ALLOW YOU TO POST TO BASICALLY ANOTHER PAGE! YOU COULD PLACE IT 
IN ANOTHER PAGE CALLED REVIEWS! MAKE SURE YOU GET THE IMAGE FIRST TO SHOW UP THOUGH AND THEN CREATE SEPERATE BUTTONS
THAT WOULD ALLOW YOU TO POST THE REVIEWS IN THE REVIEW PAGE IF POSSIBLE!-->


  </section>

    
    <div class="row">

        <!-- LOCAL INFORMATION -->
        <div class="col-sm-6">
            
        </div>
        <div class="col-sm-6" id ="messages">
         
          <h3 class ="general">GENERAL</h3>

           <ul class="messages">
            <% for(var i=0; i<messages.length; i++) {%>
              <% if(messages[i].liked===false && messages[i].hated===false){ %>
              <li class="message">  
                <span id="bookCover"><%= messages[i].bookName %></span>
                <span id="displayUrl" class ="hidden"><%= messages[i].url %></span>
                <img id="bookCoverGang" src="<%= messages[i].url %>"></img>
                <span><i class="fa-solid fa-heart" aria-hidden="true"></i></span>
                <span><i class="fa-solid fa-heart-crack" aria-hidden="true"></i></span>
                <span><i class="fa fa-ban" aria-hidden="true"></i></span>
              </li>
            <% } %>
            <% } %>

            </ul>
            </div>


  
      
          <div class="col-sm-6" id="messages">
            <h3 class ="liked">LIKED</h3>
            <ul class="messages">
              <% for(var i=0; i<messages.length; i++) {%>
                <% if(messages[i].liked===true){ %>
                  <li class="message">
                    <span id="bookCover" class ="hidden">
                      <%= messages[i].url %>
                    </span>
                    <img id="bookCover" src="<%= messages[i].url %>"></img>
                    <span><i class="fa fa-ban" aria-hidden="true"></i></span>
                  </li>
                  <% } %>
                <% } %>
            </ul>
          </div>

         
         
        <div class="col-sm-6" id ="messages">
      
          <h3 class ="hated">HATED</h3>
           <ul class="messages">
            <% for(var i=0; i<messages.length; i++) {%>
              <% if(messages[i].hated===true){ %>
              <li class="message">
                <span id="bookCover" class ="hidden"><%= messages[i].url %></span>
                <img id="bookCover" src="<%= messages[i].url %>"></img>

                <span><i class="fa fa-ban" aria-hidden="true"></i></span>
              </li>
            <% } %>
            <% } %>
            </ul>
            </div>


         
            </div>

         

</div>

<script src="main.js"></script>

</body>
</html>
